{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: 'Title'|t('app'),
    name: 'title',
    value: message.title
}) }}

{{ forms.textareaField({
    label: 'Content'|t('app'),
    name: 'content',
    value: message.content
}) }}